#include <stdio.h>
/**
 * print_to_98 - prints all natural numbers from input to 98,
 *               in order seeperate by a comma followed by space
 * @n: The number to begin counting at.
 * {
 * if (n >= 98)
 * {
 * while (n > 98)
 * printf("%d, ", n--);
 * print("%\n, n);
 * }
 *
 * else
 * {
 * while (n < 98)
 * printf("%d, ", n++);
 * }
 * }

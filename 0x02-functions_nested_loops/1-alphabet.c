#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "header.h"

/**
 * print_alphabet - Print alphabet in lowercase using only `_putchar`
 *
 * Return: Always 0 (Suc
 */
void print_alphabet(void)
{
char {c = 'a';}

while (c <= 'z')
{
putchar(c);
c++;
}
putchar('\n');
}

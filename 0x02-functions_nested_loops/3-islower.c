#include "main.h"

/**
 * main - Entry point
 *
 * Description: check if the character is lowercase
 *
 * Return: returns 1 and 0 depending on condition
 */
int _islower(void);
{
return (c >= 'a' && c <= 'Z');
}
